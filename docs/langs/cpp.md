# **MetalGPT 2.0 – C++-ish Commands**

**Tagline:** *“Strong types, classes, and epic metal – now in C++ style!”*

These commands let you write **C++-inspired code** in MetalGPT, while ChatGPT simulates outputs humorously and dramatically.

---

## **1. Variable Declaration**

**Syntax:**

```metal
<int|float|string|bool> <variable> = <value>
```

**Description:**

* Statically types variables like C++.
* Numbers, strings, and booleans supported.
* ChatGPT can simulate strict typing humorously.

**Examples:**

```metal
int HP = 100
float DAMAGE = 75.5
string HERO_NAME = "SKIBIDI"
bool IS_ALIVE = true
```

**Humor Tip:**
Use absurdly epic types like `int DRAGON_CRUSHER = 9001`.

---

## **2. Print / Output (C++ style)**

**Syntax:**

```metal
COUT << <expression> << ENDL
```

**Description:**

* C++-style console output using `COUT`.
* ChatGPT simulates the print with epic metal flavor.

**Example:**

```metal
COUT << "HERO HP: " << HP << ENDL
COUT << HERO_NAME << " SCREAMS IN GLORIOUS METAL!" << ENDL
```

**Humor Tip:**
Add `<< " 🤘🔥"` to output epic metal emojis.

---

## **3. Functions / Methods**

**Syntax:**

```metal
<return_type> <function_name>(<parameters>) {
    // block
    return <value>
}
```

**Description:**

* C++-style function definitions with return type.
* ChatGPT simulates calling functions and returning results.

**Example:**

```metal
int ATTACK(string player, int enemy_hp) {
    int DAMAGE = 50
    COUT << player << " STRIKES THE ENEMY FOR " << DAMAGE << ENDL
    return enemy_hp - DAMAGE
}

DRAGON_HP = ATTACK(HERO_NAME, DRAGON_HP)
```

---

## **4. Classes & Objects**

**Syntax:**

```metal
class <ClassName> {
    <access_modifier> <type> <member>;
    <methods>
};
```

**Description:**

* Create structured objects with members and methods.
* ChatGPT simulates object interactions in battles or chaos.

**Example:**

```metal
class Hero {
public:
    string name;
    int HP;
    void SCREAM() {
        COUT << name << " GOES METAL 🤘🔥" << ENDL
    }
};

Hero SKIBIDI
SKIBIDI.name = "SKIBIDI"
SKIBIDI.HP = 9001
SKIBIDI.SCREAM()
```

**Humor Tip:**
Create overpowered classes like `class DRAGON_DESTROYER` or `class CHAOS_WARRIOR`.

---

## **5. Operators**

**Syntax / Commands:**

* Arithmetic: `+ - * / %`
* Comparison: `== != > < >= <=`
* Logical: `&& || !`
* Assignment: `= += -= *= /= %=`

**Examples:**

```metal
HP -= 70
if (HP <= 0) {
    COUT << HERO_NAME << " IS DEVOURER!" << ENDL
}
```

**Humor Tip:**
Combine operators in absurd expressions:

```metal
TOTAL_MAYHEM = HP * DAMAGE / 9000 + 42
```

---

## **6. Loops**

### FOR Loop

```metal
for (int TURN = 1; TURN <= 10; TURN++) {
    COUT << "TURN " << TURN << ENDL
    COUT << HERO_NAME << " ATTACKS!" << ENDL
}
```

### WHILE Loop

```metal
while (HP > 0 && DRAGON_HP > 0) {
    DRAGON_HP -= 50
    HP -= 70
    COUT << "METAL BATTLE CONTINUES!" << ENDL
}
```

**Humor Tip:**
Loops are perfect for “epic battle simulations” narrated by ChatGPT.

---

## **7. Conditional Statements**

**Syntax:**

```metal
if (<condition>) {
    // block
} else if (<condition>) {
    // block
} else {
    // block
}
```

**Example:**

```metal
if (HP > 9000) {
    COUT << "IT'S OVER 9000!!! 🤘🔥" << ENDL
} else {
    COUT << "NOT METAL ENOUGH..." << ENDL
}
```

---

## **8. Arrays & Vectors**

**Arrays:**

```metal
int INVENTORY[3] = {1, 2, 3}
COUT << INVENTORY[0] << ENDL
```

**Vectors (dynamic arrays):**

```metal
vector<string> ITEMS = {"SWORD", "SHIELD", "POTION"}
COUT << ITEMS[0] << ENDL
```

**Humor Tip:**
Store ridiculous items like `"DRAGON TOOTH"` or `"CUP OF CHAOS"` for maximum metal storytelling.

---

## **9. Debugging / Simulation Helpers**

* **DEBUG(<variable>)** – Prints variable with sarcastic commentary.
* **ROCKOUT(<string>)** – Outputs dramatic metal-style text.
* **SUMMON(<entity>)** – Summons AI-simulated chaotic entities.

**Example:**

```metal
DEBUG(HP)
ROCKOUT("HERO GOES OVER 9000 HP!!! 🤘🔥")
SUMMON("DEMON")
```

---

✅ **C++-ish Summary:**

* Strongly typed variables
* Functions, classes, and objects
* Loops, conditionals, and arrays/vectors
* Humor and epic metal flavor integrated
* Perfect for AI-simulated storytelling battles
